machine:
  java:
    version: oraclejdk8
  node:
    version: 5.2.0

dependencies:
  cache_directories:
    - "node_modules"
    - "~/.npm"

    # Cache npm deps
    # Cache Selenium & ChromeDriver
    # - ./node_modules/.bin/chimp --path=content # there are no tests in content

test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/cucumber/

  override:
    - chimp --browser=chrome --singleSnippetPerFile=1 --jsonOutput=cucumber/chimpTests.cucumber
        # environment:
        #   CUCUMBER_JSON_OUTPUT: "$CIRCLE_TEST_REPORTS/cucumber/chimpTests.json"
